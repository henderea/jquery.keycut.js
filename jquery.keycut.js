(function($){
    function KeyCut(obj, options) {
    }
    function KeyCutManager(obj) {
        this.obj = obj
    }
    $.fn.keycut = function(name, options) {
        this.each(function(obj) {

        });
    }
})(jQuery);